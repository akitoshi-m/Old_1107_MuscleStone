.user-follow-wrapper
  .container
    .row
      aside.col-md-8.col-md-offset-2
        .user-follow-info
          h1.text-center
            = @user.name
            | さんのフォロー状況
        section.stats
          = render 'shared/stats'
          - if @users.any?
            .user_avatars
              - @users.each do |user|
                = link_to user.name , user
      .col-md-8
        h3
          = @title
        - if @users.any?
